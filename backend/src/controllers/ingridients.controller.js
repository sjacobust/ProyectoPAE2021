const db = require('./db.controller');
if(process.env.NODE_ENV==='dev') {
  require('dotenv').config();
}

class IngridientsController {

    getIngridients(req, res) {
        
    }

    selectedIngridients(req, res) {
        
    }


}