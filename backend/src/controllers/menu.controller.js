const db = require('./db.controller');
if(process.env.NODE_ENV==='dev') {
  require('dotenv').config();
}

class MenuController {

    updateMenu(req, res) {
        
    }

    getMenu(req, res) {
        
    }


}